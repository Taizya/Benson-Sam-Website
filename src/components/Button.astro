---
interface Props {
  className?: string;
  text?: string;
}

const { className, text = 'Learn More' } = Astro.props;
---

<div>
   <button type="button" class:list={["button", className]}>{text}</button>
</div>
